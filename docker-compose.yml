version: "3.8"
services:
  tianyan:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MIMO_API_KEY=${MIMO_API_KEY}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
